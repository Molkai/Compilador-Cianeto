// Isabela Sayuri Matsumoto - 726539 isabelasassa@hotmail.com
@cep(5, "operadores de comparação não podem ser usados em sequência","'{' expected", "comp.Compiler.ifStatement()")

class Program

    func run{
        var Int a, b, c;
        if a < b < c {
            Out.println: a;
        }

    }

end
