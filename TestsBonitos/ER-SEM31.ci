/*
    Caroline Aparecida
    hicaarol@gmail.com
*/

@cep(9, "Metodo com nome igual a de variavel de instancia",
   "Method 'i' has name equal to an instance variable",
   "comp.Compiler.methodDec()")


class A

    var Int i;

    func i {
    }

end

class Program

    func run {
        var A a;
        a = A.new;
        a.i;
    }

end


