@cep(7, "Redeclaracao da variavel  'i'", "Variable 'i' is being redeclared",
  "comp.Compiler.localDec()")

class Program

    func run {
       var Int i;
       var Int i;   //#

       i = In.readInt;
       Out.println: i;
    }

end
